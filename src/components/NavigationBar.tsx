import * as React from 'react';
import './NavigationBar.css'

const NavigationBar = () => {
    return (
        <nav>
            <h3>Easy Dashboard</h3>
        </nav>
    );
}

export default NavigationBar;